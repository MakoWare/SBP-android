<resources>

    <!-- Base application theme. -->
    <style name="MaterialDarkTheme" parent="android:Theme.Material">
        <item name="android:colorPrimary">@color/lightOrange</item>
        <item name="android:colorPrimaryDark">@color/darkOrange</item>
        <item name="android:textColorPrimary">@color/white</item>

    </style>

</resources>
